require 'rails_helper'

describe School do

		context '#do_the_math' do
			it 'should return the average cost per megabit' do
				expect(sum_of_purchases).to eq()
			end
		end

	  context '#build_chart'	do
	  	it 'should return a hash' do
	  		expect(@furious_7.formatted_release_date).to eq(Date.today.strftime("%B %e, %Y"))
	  	end
	  end
end